apiVersion: v1
kind: Service
metadata:
  name: {{ include "edemium-hotel.fullname" . }}
  labels:
    {{- include "edemium-hotel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edemium-hotel.selectorLabels" . | nindent 4 }}
