apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: gateway
        image: api-gateway:latest
        imagePullPolicy: Never
        env:
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:mysql://mysql-db:3306/persistent_login_db?useSSL=false&serverTimezone=UTC"
        - name: SPRING_DATA_REDIS_HOST
          value: "redis-cache"
        - name: USER_SERVICE_URL
          value: "http://demo-user-service:8086"
        - name: BOOKING_SERVICE_URL
          value: "http://demo-hotel-service:8087"
        - name: DATA_BASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edemium-credentials
              key: db-password
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: edemium-credentials
              key: jwt-secret-key
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: edemium-credentials
              key: redis-password
        - name: REMEMBER_ME_KEY
          valueFrom:
            secretKeyRef:
              name: edemium-credentials
              key: remember-me-key
        ports:
        - containerPort: 8085
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30085